<% @posts.each do |post|  %>
	<h2><%= post.title %></h2>
	<div id= postBox><%= truncate(post.body, :length => 250) %></div>
	<br>
	<%= link_to "Read Post", post %>

	<% end %>
<link rel="stylesheet" href="/assets/stylesheets/styles.css"
